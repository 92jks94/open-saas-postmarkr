/**
 * Timing constants for UI interactions and polling
 * Used primarily on the client side
 */

/**
 * UI debounce delays (milliseconds)
 */
export const DEBOUNCE_DELAY_MS = 2000;
export const QUICK_DEBOUNCE_MS = 1000;

/**
 * File status polling interval
 */
export const FILE_STATUS_POLL_INTERVAL_MS = 2000;

/**
 * Cookie consent expiration
 */
export const COOKIE_CONSENT_EXPIRY_DAYS = 365;

/**
 * Time conversion helpers
 */
export const SECONDS_PER_MINUTE = 60;
export const MS_PER_SECOND = 1000;
export const MS_PER_MINUTE = 60 * 1000;

