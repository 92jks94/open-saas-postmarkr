# fly.toml app configuration file generated for postmarkr-client on 2025-10-01T09:07:02-05:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'postmarkr-server-client'
primary_region = 'ord'

[build]

[env]
  PORT = "8043"
  NODE_ENV = "production"
  VITE_SENTRY_DSN = "https://ea1a698c4ecd0ccd0859b624c70f55550@o4510025126051840.ingest.us.sentry.io/4510077051535360"
  VITE_GOOGLE_ANALYTICS_ID = "G-6H2SB3GJDW"  

[http_service]
  internal_port = 8043
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  # Health check configuration
  # Note: Client health checks are handled by the server
  # The client serves static files, so we check for a successful response
  [[http_service.checks]]
    grace_period = "30s"
    interval = "30s"
    method = "GET"
    timeout = "5s"
    path = "/"
    
    [http_service.checks.headers]
      User-Agent = "Fly-Health-Check"

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
